####################Database Configuration
db.driver=org.postgresql.Driver

#db.url=jdbc:postgresql://localhost:5432/projecttonghop
#db.username=postgres
#db.password=root

 db.url=jdbc:postgresql://ec2-52-202-152-4.compute-1.amazonaws.com:5432/d16osek8uklqt1
 db.username=bjefyettudyzfm
 db.password=40f340e9352486f794fd052afb55469464c338c4fa21a9d030043de92e2d9886
 
####################Hibernate Configuration
# The dialect specifies the type of database used in hibernate so that hibernate generate appropriate type of SQL statements.
# For connecting any hibernate application with the database, it is required to provide the configuration of SQL dialect.
hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# validate: validate the schema, makes no changes to the database.
# update: update the schema.
# create: creates the schema, destroying previous data.
# create-drop: drop the schema when the SessionFactory is closed explicitly, typically when the application is stopped.
hibernate.hbm2ddl.auto=validate

hibernate.ejb.naming_strategy=org.hibernate.cfg.ImprovedNamingStrategy

hibernate.show_sql=true
hibernate.format_sql=true

hibernate.jdbc.lob.non_contextual_creation=true
